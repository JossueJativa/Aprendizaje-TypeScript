const name: string = 'Jossue';
let hpPoints: number | 'FULL' = 100;
const isAlive: boolean = true;

hpPoints = 'FULL';

console.log(
    `Name: ${name}, HP: ${hpPoints}, Alive: ${isAlive}`
);

export {};